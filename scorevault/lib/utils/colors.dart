import 'package:flutter/material.dart';

abstract class AppColors {
  // Light Theme
  static const lightPrimary = Color.fromARGB(255, 255, 122, 28);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightGrey = Color.fromARGB(255, 240, 240, 240);

  // Dark Theme
    static const darkPrimary = Color.fromARGB(255, 255, 122, 28);
  static const darkSurface = Color.fromARGB(0, 27, 27, 27);
    static const darkGrey = Color.fromARGB(255, 50, 50, 50);

  // common
  static const red = Color.fromARGB(213, 231, 64, 64);
  static const green = Color.fromARGB(211, 64, 231, 103);

}
